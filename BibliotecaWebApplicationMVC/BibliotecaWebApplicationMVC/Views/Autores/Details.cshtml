@model BibliotecaWebApplicationMVC.Models.Autor

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
    <h4>Autor</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nombres)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nombres)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Apellidos)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Apellidos)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nacionalidad)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nacionalidad)
        </dd>
        @* <dt class="col-sm-2">
            Foto
        </dt>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.FotoUrl))
            {
                <img src="@Model.FotoUrl" alt="Foto del Autor" style="max-width: 150px;" />
            }
            else
            {
                <span>No hay foto disponible.</span>
            }
        </dd> *@

    </dl>
</div>
<br />
<div>
    <a asp-action="Edit" asp-route-id="@Model?.AutorId" class="btn btn-edit"><i class="fas fa-edit"></i> Editar</a>
    <a asp-action="Index" class="btn btn-back"><i class="fas fa-arrow-left"></i> Volver</a>
</div>
